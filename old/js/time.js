var agus = document.getElementById("agus");
var indonesia = document.getElementById("indonesia");
var mexico = document.getElementById("mexico");
var dalmo = document.getElementById("dalmo");
var dastrevas = document.getElementById("dastrevas");
var devil = document.getElementById("devil");
var dulce = document.getElementById("dulce");
var eye = document.getElementById("eye");
var gtk = document.getElementById("gtk");
var jrguerra = document.getElementById("jrguerra");
var karkara = document.getElementById("karkara");
var king = document.getElementById("king");
var leleco = document.getElementById("leleco");
var markoz = document.getElementById("markoz");
var messi = document.getElementById("messi");
var nicko = document.getElementById("nicko");
var palo = document.getElementById("palo");
var soto = document.getElementById("soto");
var tropa = document.getElementById("tropa");
var venezuela = document.getElementById("venezuela");
var wiracocha = document.getElementById("wiracocha");
var zesereno = document.getElementById("zesereno");
var corisco = document.getElementById("corisco");
var yep = document.getElementById("yep");



function horaAgus() {
  var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Argentina/Buenos_Aires"});
  var formattedString = dateString.replace(", ", " - ");
  agus.innerHTML = formattedString;
}
function horaIndonesia() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "Asia/Jakarta"});
    var formattedString = dateString.replace(", ", " - ");
    indonesia.innerHTML = formattedString;
  }
function horaMexico() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "Mexico/General"});
    var formattedString = dateString.replace(", ", " - ");
    mexico.innerHTML = formattedString;
  }
  function horaDalmo() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Argentina/Buenos_Aires"});
    var formattedString = dateString.replace(", ", " - ");
    dalmo.innerHTML = formattedString;
  }
  function horaDasTrevas() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Sao_Paulo"});
    var formattedString = dateString.replace(", ", " - ");
    dastrevas.innerHTML = formattedString;
  }
  function horaDevil() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "Asia/Yangon"});
    var formattedString = dateString.replace(", ", " - ");
    devil.innerHTML = formattedString;
  }
  function horaDulce() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "Mexico/General"});
    var formattedString = dateString.replace(", ", " - ");
    dulce.innerHTML = formattedString;
  }
  function horaEye() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "Mexico/General"});
    var formattedString = dateString.replace(", ", " - ");
    eye.innerHTML = formattedString;
  }
  function horaGtk() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "Mexico/General"});
    var formattedString = dateString.replace(", ", " - ");
    gtk.innerHTML = formattedString;
  }
  function horaJrguerra() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Caracas"});
    var formattedString = dateString.replace(", ", " - ");
    jrguerra.innerHTML = formattedString;
  }
  function horaKarkara() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Sao_Paulo"});
    var formattedString = dateString.replace(", ", " - ");
    karkara.innerHTML = formattedString;
  }
  function horaKing() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Lima"});
    var formattedString = dateString.replace(", ", " - ");
    king.innerHTML = formattedString;
  }
  function horaLeleco() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Sao_Paulo"});
    var formattedString = dateString.replace(", ", " - ");
    leleco.innerHTML = formattedString;
  }
  function horaMarkoz() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Montevideo"});
    var formattedString = dateString.replace(", ", " - ");
    markoz.innerHTML = formattedString;
  }
  function horaMessi() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Argentina/Buenos_Aires"});
    var formattedString = dateString.replace(", ", " - ");
    messi.innerHTML = formattedString;
  }
  function horaNicko() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Montevideo"});
    var formattedString = dateString.replace(", ", " - ");
    nicko.innerHTML = formattedString;
  }
  function horaPalo() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/La_Paz"});
    var formattedString = dateString.replace(", ", " - ");
    palo.innerHTML = formattedString;
  }
  function horaSoto() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Argentina/Buenos_Aires"});
    var formattedString = dateString.replace(", ", " - ");
    soto.innerHTML = formattedString;
  }
  function horaTropa() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Sao_Paulo"});
    var formattedString = dateString.replace(", ", " - ");
    tropa.innerHTML = formattedString;
  }
  function horaVenezuela() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Caracas"});
    var formattedString = dateString.replace(", ", " - ");
    venezuela.innerHTML = formattedString;
  }
  function horaWiracocha() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Lima"});
    var formattedString = dateString.replace(", ", " - ");
    wiracocha.innerHTML = formattedString;
  }
  function horaZesereno() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Sao_Paulo"});
    var formattedString = dateString.replace(", ", " - ");
    zesereno.innerHTML = formattedString;
  }
  function horaCorisco() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "America/Sao_Paulo"});
    var formattedString = dateString.replace(", ", " - ");
    corisco.innerHTML = formattedString;
  }
  function horaYep() {
    var dateString = new Date().toLocaleTimeString("en-US", {timeZone: "Europe/Kiev"});
    var formattedString = dateString.replace(", ", " - ");
    yep.innerHTML = formattedString;
  }

  




    setInterval(horaAgus, 1000);
    setInterval(horaIndonesia, 1000);
    setInterval(horaMexico, 1000);
    setInterval(horaDalmo, 1000);
    setInterval(horaDasTrevas, 1000);
    setInterval(horaDevil, 1000);
    setInterval(horaDulce, 1000);
    setInterval(horaEye, 1000);
    setInterval(horaGtk, 1000);
    setInterval(horaJrguerra, 1000);
    setInterval(horaKarkara, 1000);
    setInterval(horaKing, 1000);
    setInterval(horaLeleco, 1000);
    setInterval(horaMarkoz, 1000);
    setInterval(horaMessi, 1000);
    setInterval(horaNicko, 1000);
    setInterval(horaPalo, 1000);
    setInterval(horaSoto, 1000);
    setInterval(horaTropa, 1000);
    setInterval(horaVenezuela, 1000);
    setInterval(horaWiracocha, 1000);
    setInterval(horaZesereno, 1000);
    setInterval(horaCorisco, 1000);
    setInterval(horaYep, 1000);

    